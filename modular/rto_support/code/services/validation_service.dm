/// Future validation service for zone deployment and support calls.
/// Skeleton stage: performs no validation.
/datum/rto_support_validation_service

/// Validates a future visibility zone deployment attempt.
/// Skeleton stage: intentionally returns FALSE.
/datum/rto_support_validation_service/proc/validate_zone_deploy(...)
	return FALSE

/// Validates a future support call attempt.
/// Skeleton stage: intentionally returns FALSE.
/datum/rto_support_validation_service/proc/validate_support_call(...)
	return FALSE
